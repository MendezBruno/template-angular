import { FunctionSatisfyDirective } from './function-satisfy.directive';

describe('FunctionSatisfyDirective', () => {
  it('should create an instance', () => {
    const directive = new FunctionSatisfyDirective();
    expect(directive).toBeTruthy();
  });
});
