import { dosDecimalFormatDirective } from "./2decimales-format.directive";


describe('dosDecimalFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new dosDecimalFormatDirective();
    expect(directive).toBeTruthy();
  });
});