import { PriceFormaDirective } from './price-forma.directive';

describe('PriceFormaDirective', () => {
  
  it('should create an instance', () => {
    // const directive = new PriceFormaDirective();
    // expect(directive).toBeTruthy();
  });
});
