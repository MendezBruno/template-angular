import { Decimal159ValidatorDirective } from './decimal15-9-validator.directive';

describe('Decimal159ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Decimal159ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
